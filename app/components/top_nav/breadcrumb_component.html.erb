<li>
  <%= link_to @name, @path, 
            class: ["block sm:bg-transparent py-2 px-5 sm:px-1 rounded-sm md:p-0 font-bold #{helpers.hoverBrandColors}",
                    ("text-activelink" if helpers.active?(@path))],
            aria: { current: helpers.active?(@path) ? 'current' : 'false' } %>
</li>