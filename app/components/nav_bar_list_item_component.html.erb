<li>
  <%= link_to @name, @path, 
            class: ["block py-2 px-3 rounded-sm md:p-0 font-bold #{helpers.hoverBrandColors}",
                    ("text-activelink" if helpers.active?(@path))],
            aria: { current: helpers.active?(@path) ? 'current' : 'false' } %>
</li>